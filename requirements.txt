# --- Core web + data ---
flask>=3.0
sqlalchemy>=2.0
requests
apscheduler

# --- Data science / ML ---
pandas
numpy
scipy
scikit-learn
xgboost
lightgbm
statsmodels

# --- Reports ---
openpyxl
reportlab

# --- Historical/live data sources (build plan §2) ---
soccerdata            # primary historical/advanced-metrics source (FBref-backed)
# ScraperFC            # optional enrichment (Transfermarkt market values) — uncomment
#                       # and set EPL_ENABLE_SCRAPERFC=true to use it

# --- Config ---
python-dotenv

# --- Production WSGI server (only needed if EPL_WSGI_SERVER=production) ---
waitress

# --- Optional: NLP layer (engine/huggingface_models.py) ---
# transformers
# torch

# --- Dev / testing ---
pytest
