"""
Prediction engine package.

engine/probability_model.py is the core — a direct, validated port of the
Dixon-Coles bivariate Poisson maths from the original React prototype
(same formulas, same variable names where practical, so anyone diffing
against the JS version can follow along).
"""
